<%= simple_form_for([recipe, measure], remote: true, method: 'post') do |f| %>
  <%= f.input :description, label: false, placeholder: "Quantity" %>
  <%= f.association :ingredient,label: false %>
  <%= f.submit "Add Measure", class: "btn btn-primary" %>
<% end %>
